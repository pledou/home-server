# Offline fallback mode for {{ app_domain_name }}.
# Redirect root and subdomains to local services when internet is unavailable.
local-zone: "{{ app_domain_name }}." redirect
include: /etc/unbound/a-records.conf
include: /etc/unbound/srv-records.conf
