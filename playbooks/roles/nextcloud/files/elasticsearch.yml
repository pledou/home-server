# Elasticsearch configuration for single-node Nextcloud setup
# This file is managed by Ansible - do not edit manually

cluster.name: "docker-cluster"
network.host: 0.0.0.0

# Single-node setup: disable shard allocation awareness
# Note: index.number_of_replicas CANNOT be set here (index-level setting)
# Use index templates instead (applied via API after startup)
